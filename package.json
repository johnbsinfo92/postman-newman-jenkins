{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests": "newman run tests/Jsonplaceholder.com.postman_collection.json --reporter junit -junit-export newman.xml"
  },
  "author": "Jhonn B",
  "dependencies": {
    "newman": "^3.9.4"
  }
}
